plugins {}

group 'rsbox'
version '1.0'

apply plugin: 'application'

description = 'RSBox Server'
mainClassName = 'io.rsbox.server.Launcher'

dependencies {
    compile project(':server:net')
}

run {
    workingDir = rootProject.projectDir
}

configure(run) {
    group = 'server'
}